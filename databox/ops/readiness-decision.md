# DBX-28 — Operational & release readiness decision

**Gate:** DBX-28 — runbooks, SBOM, dependency/secret scan, hardening checklist, key-ceremony +
restore rehearsals, signed readiness decision. **Decision: READY (with named residual risks).**

## Evidence

- **Rehearsals pass** — `test/unit/databox/ops/OpsRehearsal.test.ts`: encrypted backup restores
  exact resources; owner-key backup recovers only under the holder's private key; the signing-key
  ceremony→rotation→revocation lifecycle resolves in-window and fails closed on retired/revoked/
  substituted keys. (3/3)
- **SBOM** — `databox/ops/sbom.cyclonedx.json` (CycloneDX 1.5), generated by
  `scripts/generate-sbom.js` from production dependencies only.
- **Dependency scan** — `npm audit --omit=dev`: **0 production vulnerabilities** after `audit fix`
  (undici 8.x advisories + brace-expansion resolved). Remaining findings are dev-only tooling.
- **Secret scan** — `scripts/secret-scan.js`: clean (a demo-UI token literal was the only hit —
  entropy-free placeholder, not a secret).
- **Hardening checklist** — `hardening-checklist.md`: authorization boundary, secrets/keys,
  transport, durability, privacy controls each marked verified/open.
- **Runbooks** — `runbooks.md`: key ceremony/rotation, tenant onboarding, backup/restore, incident
  response, relationship recovery, duty monitoring, retention.
- **Prior gates green** — DBX-26 deployment-tier P1s (5/5), DBX-27 interop conformance (7/7), 199
  suites / 1905 tests.

## Residual risks (named, owned)

| Risk | Owner | Disposition |
|---|---|---|
| Production signing-key custody is operator-held, not KMS/HSM | CIV-C10 | Accept for pilot; KMS custody tracked under production hardening |
| Registries/outbox/evidence are process-local in the reference preset | CIV-C10 | Durable stores + WORM ledger tracked; not on this release path |
| Dev-dependency audit findings (eslint/brace-expansion) | release | Accepted — dev tooling is not shipped; re-scan on upgrade |
| TLS/DNS termination topology is documented not deployed (ADR-0027) | deployment | Deployer responsibility per hosting model |

**Signed:** readiness decision recorded for the DBX-28 release gate.
